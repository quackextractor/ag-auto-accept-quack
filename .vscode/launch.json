{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Extension with CDP",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "--remote-debugging-port=9222"
            ]
        }
    ]
}